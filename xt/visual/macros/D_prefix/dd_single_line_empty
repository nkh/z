{
    desc => q{dd on single line leaves empty buffer},
    description => <<'END_DESC',
# dd_single_line_empty, dd on single line leaves empty buffer

Macro: `xt/visual/macros/D_prefix/dd_single_line_empty`

## initial.png

- Cursor: 1, 0
- Line 1  : only line here

## after_dd_empty.png

- Keys    : dd


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('dd');
        $ctx->delay(100);
        $ctx->snapshot('after_dd_empty');
    },
}
__DATA__
only line here
