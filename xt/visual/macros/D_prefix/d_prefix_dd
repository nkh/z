{
    desc => q{dd deletes current line},
    description => <<'END_DESC',
# d_prefix_dd, dd deletes current line

Macro: `xt/visual/macros/D_prefix/d_prefix_dd`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three

## after_dd.png

- Keys    : jdd


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('jdd');
        $ctx->delay(100);
        $ctx->snapshot('after_dd');
    },
}
__DATA__
line one
line two
line three
