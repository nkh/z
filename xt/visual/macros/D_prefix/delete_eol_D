{
    desc => q{D deletes to end of line},
    description => <<'END_DESC',
# delete_eol_D, D deletes to end of line

Macro: `xt/visual/macros/D_prefix/delete_eol_D`

## initial.png

- Cursor: 1, 0
- Line 1  : This is a first line of text in the buffer.
- Line 2  : Here is a second line with more content.
- Line 3  : Third line with some additional words here.
- Line 4  : Fourth line to provide scrolling context.
- Line 5  : Fifth line at the bottom of the buffer.

## cursor_before_D.png

- Keys    : 6l

## after_D.png

- Keys    : D


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('6l');
        $ctx->delay(100);
        $ctx->snapshot('cursor_before_D');
        $ctx->delay(100);
        $ctx->keys('D');
        $ctx->delay(100);
        $ctx->snapshot('after_D');
    },
}
__DATA__
This is a first line of text in the buffer.
Here is a second line with more content.
Third line with some additional words here.
Fourth line to provide scrolling context.
Fifth line at the bottom of the buffer.
