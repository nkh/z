{
    desc => q{r replaces single character},
    description => <<'END_DESC',
# replace_word, r replaces single character

Macro: `xt/visual/macros/replace/replace_word`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## after_replace.png

- Keys    : 4lrO


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('4lrO');
        $ctx->delay(100);
        $ctx->snapshot('after_replace');
    },
}
__DATA__
hello world
