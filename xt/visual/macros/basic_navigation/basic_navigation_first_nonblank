{
    desc => q{First non-blank ^ motion},
    description => <<'END_DESC',
# basic_navigation_first_nonblank, First non-blank ^ motion

Macro: `xt/visual/macros/basic_navigation/basic_navigation_first_nonblank`

## initial.png

- Cursor: 1, 0
- Line 1  :   hello world
- Line 2  :   foo bar

## after_action.png

- Keys    : ^


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('^');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
  hello world
  foo bar

