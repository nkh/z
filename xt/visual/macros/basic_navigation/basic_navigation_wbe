{
    desc => q{Word motion w/b/e},
    description => <<'END_DESC',
# basic_navigation_wbe, Word motion w/b/e

Macro: `xt/visual/macros/basic_navigation/basic_navigation_wbe`

## initial.png

- Cursor: 1, 0
- Line 1  : one two three four
- Line 2  : five six seven

## after_action.png

- Keys    : w


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('w');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
one two three four
five six seven

