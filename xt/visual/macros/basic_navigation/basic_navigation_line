{
    desc => q{Line start/end motions $ and 0},
    description => <<'END_DESC',
# basic_navigation_line, Line start/end motions $ and 0

Macro: `xt/visual/macros/basic_navigation/basic_navigation_line`

## initial.png

- Cursor: 1, 0
- Line 1  : the quick brown fox jumps
- Line 2  : short
- Line 3  : another line with more text

## after_dollar.png

- Keys    : $

## after_zero.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$');
        $ctx->delay(100);
        $ctx->snapshot('after_dollar');
        $ctx->delay(100);
        $ctx->keys('0');
        $ctx->delay(100);
        $ctx->snapshot('after_zero');
    },
}
__DATA__
the quick brown fox jumps
short
another line with more text
