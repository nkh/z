{
    desc => q{Basic h/j/k/l cursor movement},
    description => <<'END_DESC',
# basic_navigation_hjkl, Basic h/j/k/l cursor movement

Macro: `xt/visual/macros/basic_navigation/basic_navigation_hjkl`

## initial.png

- Cursor: 1, 0
- Line 1  : Hello world
- Line 2  : foo bar baz
- Line 3  : one two three
- Line 4  : alpha beta
- Line 5  : gamma delta

## after_ll.png

- Keys    : ll

## after_jj.png

- Keys    : jj

## after_hk.png

- Keys    : hk


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('ll');
        $ctx->delay(100);
        $ctx->snapshot('after_ll');
        $ctx->delay(100);
        $ctx->keys('jj');
        $ctx->delay(100);
        $ctx->snapshot('after_jj');
        $ctx->delay(100);
        $ctx->keys('hk');
        $ctx->delay(100);
        $ctx->snapshot('after_hk');
    },
}
__DATA__
Hello world
foo bar baz
one two three
alpha beta
gamma delta
