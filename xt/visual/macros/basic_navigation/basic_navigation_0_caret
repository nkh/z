{
    desc => q{Line start 0 and first-non-blank ^},
    description => <<'END_DESC',
# basic_navigation_0_caret, Line start 0 and first-non-blank ^

Macro: `xt/visual/macros/basic_navigation/basic_navigation_0_caret`

## initial.png

- Cursor: 1, 0
- Line 1  :   hello world
- Line 2  :   foo bar

## mid_dollar.png

- Keys    : $

## after_action.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$');
        $ctx->delay(100);
        $ctx->snapshot('mid_dollar');
        $ctx->delay(100);
        $ctx->keys('0');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
  hello world
  foo bar

