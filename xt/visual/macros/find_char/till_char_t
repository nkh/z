{
    desc => q{t{char} moves cursor to column before target character},
    description => <<'END_DESC',
# till_char_t, t{char

Macro: `xt/visual/macros/find_char/till_char_t`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## after_tw.png

- Keys    : tw


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('tw');
        $ctx->delay(100);
        $ctx->snapshot('after_tw');
    },
}
__DATA__
hello world
