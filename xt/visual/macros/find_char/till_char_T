{
    desc => q{T{char} moves cursor to column after target character (backward)},
    description => <<'END_DESC',
# till_char_T, T{char

Macro: `xt/visual/macros/find_char/till_char_T`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## after_To.png

- Keys    : $To


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$To');
        $ctx->delay(100);
        $ctx->snapshot('after_To');
    },
}
__DATA__
hello world
