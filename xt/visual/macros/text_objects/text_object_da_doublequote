{
    desc => q{da" deletes text including surrounding double quotes},
    description => <<'END_DESC',
# text_object_da_doublequote, da" deletes text including surrounding double quotes

Macro: `xt/visual/macros/text_objects/text_object_da_doublequote`

## initial.png

- Cursor: 1, 0
- Line 1  : say("hello world");

## after_da_doublequote.png

- Keys    : llll da"


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llll');
        $ctx->delay(50);
        $ctx->keys('da"');
        $ctx->delay(100);
        $ctx->snapshot('after_da_doublequote');
    },
}
__DATA__
say("hello world");
