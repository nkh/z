{
    desc => 'ci[ changes text inside square brackets',
    description => <<'END_DESC',
# text_object_ci_bracket, ci[ changes text inside square brackets

Macro: `xt/visual/macros/text_objects/text_object_ci_bracket`

## initial.png

- Cursor: 1, 0
- Line 1  : my @arr = [1, 2, 3];

## after_ci_bracket.png

- Keys    : llllllllllll ci[


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llllllllllll');
        $ctx->delay(50);
        $ctx->keys('ci[');
        $ctx->delay(50);
        $ctx->type('a, b, c');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_ci_bracket');
    },
}
__DATA__
my @arr = [1, 2, 3];
