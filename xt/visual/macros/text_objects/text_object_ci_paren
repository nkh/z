{
    desc => 'ci( changes text inside parentheses',
    description => <<'END_DESC',
# text_object_ci_paren, ci( changes text inside parentheses

Macro: `xt/visual/macros/text_objects/text_object_ci_paren`

## initial.png

- Cursor: 1, 0
- Line 1  : func(hello world);

## after_ci_paren.png

- Keys    : llll ci(


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llll');
        $ctx->delay(50);
        $ctx->keys('ci(');
        $ctx->delay(50);
        $ctx->type('x, y');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_ci_paren');
    },
}
__DATA__
func(hello world);
