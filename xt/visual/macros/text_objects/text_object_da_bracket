{
    desc => 'da[ deletes text including square brackets',
    description => <<'END_DESC',
# text_object_da_bracket, da[ deletes text including square brackets

Macro: `xt/visual/macros/text_objects/text_object_da_bracket`

## initial.png

- Cursor: 1, 0
- Line 1  : my @arr = [1, 2, 3];

## after_da_bracket.png

- Keys    : llllllllllll da[


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llllllllllll');
        $ctx->delay(50);
        $ctx->keys('da[');
        $ctx->delay(100);
        $ctx->snapshot('after_da_bracket');
    },
}
__DATA__
my @arr = [1, 2, 3];
