{
    desc => 'ciw changes inner word and enters insert mode',
    description => <<'END_DESC',
# text_object_ciw, ciw changes inner word and enters insert mode

Macro: `xt/visual/macros/text_objects/text_object_ciw`

## initial.png

- Cursor: 1, 0
- Line 1  : hello   world   this

## after_ciw.png

- Keys    : ciw


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('ciw');
        $ctx->delay(50);
        $ctx->type('HI');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_ciw');
    },
}
__DATA__
hello   world   this
