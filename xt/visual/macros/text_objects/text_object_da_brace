{
    desc => 'da{ delete including braces',
    description => <<'END_DESC',
Action: da{ deletes text including curly braces

Macro: xt/visual/macros/text_object_da_brace

**initial.png** (before):
- Line 1: func({ key => 'value' });
- Cursor at line 1, col 0

Keys: lllllllda{

**after_delete.png** (after):
- Line 1: func();
- Braces and content all deleted
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);

        $ctx->snapshot('after_delete');
    },
}
__DATA__
func({ key => 'value' });
