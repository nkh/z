{
    desc => 'da{ delete including braces',
    description => <<'END_DESC',
# text_object_da_brace, da{ delete including braces

Macro: `xt/visual/macros/text_objects/text_object_da_brace`

## initial.png

- Cursor: 1, 0
- Line 1  : func({ key => 'value' });

## after_delete.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);

        $ctx->snapshot('after_delete');
    },
}
__DATA__
func({ key => 'value' });
