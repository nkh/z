{
    desc => q{ca( changes text including parentheses},
    description => <<'END_DESC',
# text_object_ca_paren, ca( changes text including parentheses

Macro: `xt/visual/macros/text_objects/text_object_ca_paren`

## initial.png

- Cursor: 1, 0
- Line 1  : func(hello world);

## in_insert_mode.png

- Keys    : llllca(

## after_change.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llllca(');
        $ctx->delay(100);
        $ctx->snapshot('in_insert_mode');
        $ctx->delay(50);
        $ctx->type('REPLACED');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_change');
    },
}
__DATA__
func(hello world);
