{
    desc => 'daw deletes a word with surrounding whitespace',
    description => <<'END_DESC',
# text_object_daw, daw deletes a word with surrounding whitespace

Macro: `xt/visual/macros/text_objects/text_object_daw`

## initial.png

- Cursor: 1, 0
- Line 1  : hello   world   this

## after_daw.png

- Keys    : daw


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('daw');
        $ctx->delay(100);
        $ctx->snapshot('after_daw');
    },
}
__DATA__
hello   world   this
