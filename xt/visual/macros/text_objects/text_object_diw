{
    desc => 'diw deletes inner word without surrounding whitespace',
    description => <<'END_DESC',
# text_object_diw, diw deletes inner word without surrounding whitespace

Macro: `xt/visual/macros/text_objects/text_object_diw`

## initial.png

- Cursor: 1, 0
- Line 1  : hello   world   this

## after_diw.png

- Keys    : diw


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('diw');
        $ctx->delay(100);
        $ctx->snapshot('after_diw');
    },
}
__DATA__
hello   world   this
