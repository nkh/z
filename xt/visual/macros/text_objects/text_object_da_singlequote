{
    desc => q{da' deletes text including surrounding single quotes},
    description => <<'END_DESC',
# text_object_da_singlequote, da' deletes text including surrounding single quotes

Macro: `xt/visual/macros/text_objects/text_object_da_singlequote`

## initial.png

- Cursor: 1, 0
- Line 1  : my $x = 'hello world';

## after_da_singlequote.png

- Keys    : llllllllll


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llllllllll');
        $ctx->delay(50);
        $ctx->keys("da'");
        $ctx->delay(100);
        $ctx->snapshot('after_da_singlequote');
    },
}
__DATA__
my $x = 'hello world';
