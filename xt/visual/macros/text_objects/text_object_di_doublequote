{
    desc => q{di" deletes text inside double quotes},
    description => <<'END_DESC',
# text_object_di_doublequote, di" deletes text inside double quotes

Macro: `xt/visual/macros/text_objects/text_object_di_doublequote`

## initial.png

- Cursor: 1, 0
- Line 1  : my $msg = "hello world";

## after_di_doublequote.png

- Keys    : lllllllllll di"


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('lllllllllll');
        $ctx->delay(50);
        $ctx->keys('di"');
        $ctx->delay(100);
        $ctx->snapshot('after_di_doublequote');
    },
}
__DATA__
my $msg = "hello world";
