{
    desc => 'ci" changes text inside double quotes',
    description => <<'END_DESC',
# text_object_ci_doublequote, ci" changes text inside double quotes

Macro: `xt/visual/macros/text_objects/text_object_ci_doublequote`

## initial.png

- Cursor: 1, 0
- Line 1  : say("hello world");

## after_ci_doublequote.png

- Keys    : llll ci"


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llll');
        $ctx->delay(50);
        $ctx->keys('ci"');
        $ctx->delay(50);
        $ctx->type('replaced');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_ci_doublequote');
    },
}
__DATA__
say("hello world");
