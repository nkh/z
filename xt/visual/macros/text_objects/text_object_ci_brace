{
    desc => 'ci{ change inner braces',
    description => <<'END_DESC',
# text_object_ci_brace, ci{ change inner braces

Macro: `xt/visual/macros/text_objects/text_object_ci_brace`

## initial.png

- Cursor: 1, 0
- Line 1  : func({ key => 'value' });

## in_insert_mode.png

- Keys    : lllllllci{

## after_change.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('lllllllci{');
        $ctx->delay(100);
        $ctx->snapshot('in_insert_mode');
        $ctx->delay(50);
        $ctx->type('REPLACED');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_change');
    },
}
__DATA__
func({ key => 'value' });
