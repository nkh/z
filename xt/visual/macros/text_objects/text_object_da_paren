{
    desc => 'da( deletes text including parentheses',
    description => <<'END_DESC',
# text_object_da_paren, da( deletes text including parentheses

Macro: `xt/visual/macros/text_objects/text_object_da_paren`

## initial.png

- Cursor: 1, 0
- Line 1  : func(hello world);

## after_da_paren.png

- Keys    : llll da(


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('llll');
        $ctx->delay(50);
        $ctx->keys('da(');
        $ctx->delay(100);
        $ctx->snapshot('after_da_paren');
    },
}
__DATA__
func(hello world);
