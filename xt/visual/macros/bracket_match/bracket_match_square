{
    desc => q{Bracket match % square brackets},
    description => <<'END_DESC',
# bracket_match_square, Bracket match % square brackets

Macro: `xt/visual/macros/bracket_match/bracket_match_square`

## initial.png

- Cursor: 1, 0
- Line 1  : my @items = [1, 2, 3];

## on_bracket.png

- Keys    : 12l

## after_action.png

- Keys    : %


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('12l');
        $ctx->delay(100);
        $ctx->snapshot('on_bracket');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
my @items = [1, 2, 3];

