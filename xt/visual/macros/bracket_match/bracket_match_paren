{
    desc => q{Bracket match % parentheses},
    description => <<'END_DESC',
# bracket_match_paren, Bracket match % parentheses

Macro: `xt/visual/macros/bracket_match/bracket_match_paren`

## initial.png

- Cursor: 1, 0
- Line 1  : my ($x, $y) = foo();

## on_paren.png

- Keys    : 3l

## after_action.png

- Keys    : %


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('3l');
        $ctx->delay(100);
        $ctx->snapshot('on_paren');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
my ($x, $y) = foo();

