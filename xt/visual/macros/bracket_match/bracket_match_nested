{
    desc => q{Bracket match nested},
    description => <<'END_DESC',
Action: `%` jumps between matching brackets across nested types

Macro: `xt/visual/macros/bracket_match_nested`

**initial.png** (before):
- Line 1: `if ($x && foo($y)) {`
- Line 3: `}`
- Cursor at line 1, col 0

Keys: `3l` (move to `(`)

**on_paren.png** (cursor on `(`):
- Cursor at line 1, col 3 (on `(`)

Keys: `%`

**mid_close_paren.png** (after first `%`):
- Cursor at line 1, col 19 (on `)` matching `(` at col 3)

Keys: `%`

**after_action.png** (after second `%`):
- Cursor at line 3, col 0 (on `}` matching `{` at col 21)
- No text change

END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('3l');
        $ctx->delay(100);
        $ctx->snapshot('on_paren');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('mid_close_paren');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
if ($x && foo($y)) {
    bar();
}

