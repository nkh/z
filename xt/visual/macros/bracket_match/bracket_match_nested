{
    desc => q{Bracket match nested},
    description => <<'END_DESC',
# bracket_match_nested, Bracket match nested

Macro: `xt/visual/macros/bracket_match/bracket_match_nested`

## initial.png

- Cursor: 1, 0
- Line 1  : if ($x && foo($y)) {
- Line 2  :     bar();
- Line 3  : }

## on_paren.png

- Keys    : 3l

## mid_close_paren.png

- Keys    : %

## after_action.png

- Keys    : %


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('3l');
        $ctx->delay(100);
        $ctx->snapshot('on_paren');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('mid_close_paren');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
if ($x && foo($y)) {
    bar();
}

