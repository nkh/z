{
    desc => q{Bracket match % curly braces},
    description => <<'END_DESC',
# bracket_match_brace, Bracket match % curly braces

Macro: `xt/visual/macros/bracket_match/bracket_match_brace`

## initial.png

- Cursor: 1, 0
- Line 1  : sub foo {
- Line 2  :     my $x = 1;
- Line 3  :     return $x;
- Line 4  : }

## on_brace.png

- Keys    : 8l

## after_action.png

- Keys    : %


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('8l');
        $ctx->delay(100);
        $ctx->snapshot('on_brace');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('after_action');
    },
}
__DATA__
sub foo {
    my $x = 1;
    return $x;
}

