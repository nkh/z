{
    desc => q{% on a non-bracket character does nothing},
    description => <<'END_DESC',
# percent_not_on_bracket, % on a non-bracket character does nothing

Macro: `xt/visual/macros/bracket_match/percent_not_on_bracket`

## initial.png

- Cursor: 1, 0
- Line 1  : no brackets here
- Line 2  : just plain text

## after_percent.png

- Keys    : %


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('%');
        $ctx->delay(100);
        $ctx->snapshot('after_percent');
    },
}
__DATA__
no brackets here
just plain text
