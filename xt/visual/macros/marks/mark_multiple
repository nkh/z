{
    desc => q{Multiple marks (a, b) coexist and can be jumped between},
    description => <<'END_DESC',
# mark_multiple, Multiple marks (a, b) coexist and can be jumped between

Macro: `xt/visual/macros/marks/mark_multiple`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## after_mark_a.png

- Keys    : ma

## after_mark_b.png

- Keys    : jj mb

## after_move_down.png

- Keys    : j

## after_jump_a.png

- Keys    : `a

## after_jump_b.png

- Keys    : `b


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('ma');
        $ctx->delay(100);
        $ctx->snapshot('after_mark_a');
        $ctx->delay(100);
        $ctx->keys('jj');
        $ctx->delay(100);
        $ctx->keys('mb');
        $ctx->delay(100);
        $ctx->snapshot('after_mark_b');
        $ctx->delay(100);
        $ctx->keys('j');
        $ctx->delay(100);
        $ctx->snapshot('after_move_down');
        $ctx->delay(100);
        $ctx->keys('`a');
        $ctx->delay(100);
        $ctx->snapshot('after_jump_a');
        $ctx->delay(100);
        $ctx->keys('`b');
        $ctx->delay(100);
        $ctx->snapshot('after_jump_b');
    },
}
__DATA__
line one
line two
line three
line four
line five
