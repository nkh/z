{
    desc => q{Setting a mark overwrites the previous value at that mark},
    description => <<'END_DESC',
# mark_overwrite, Setting a mark overwrites the previous value at that mark

Macro: `xt/visual/macros/marks/mark_overwrite`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## after_first_mark.png

- Keys    : ma

## after_move_down.png

- Keys    : jjj

## after_overwrite_mark.png

- Keys    : ma

## after_jump_overwritten.png

- Keys    : `a


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('ma');
        $ctx->delay(100);
        $ctx->snapshot('after_first_mark');
        $ctx->delay(100);
        $ctx->keys('jjj');
        $ctx->delay(100);
        $ctx->snapshot('after_move_down');
        $ctx->delay(100);
        $ctx->keys('ma');
        $ctx->delay(100);
        $ctx->snapshot('after_overwrite_mark');
        $ctx->delay(100);
        $ctx->keys('`a');
        $ctx->delay(100);
        $ctx->snapshot('after_jump_overwritten');
    },
}
__DATA__
line one
line two
line three
line four
line five
