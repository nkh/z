{
    desc => q{'a jumps to the first non-blank character of the marked line},
    description => <<'END_DESC',
# mark_jump_quote, 'a jumps to the first non-blank character of the marked line

Macro: `xt/visual/macros/marks/mark_jump_quote`

## initial.png

- Cursor: 1, 0
- Line 1  :     indented line
- Line 2  : normal line

## after_set_mark.png

- Keys    : ma

## after_move_down.png

- Keys    : j

## after_quote_a.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('ma');
        $ctx->delay(100);
        $ctx->snapshot('after_set_mark');
        $ctx->delay(100);
        $ctx->keys('j');
        $ctx->delay(100);
        $ctx->snapshot('after_move_down');
        $ctx->delay(100);
        $ctx->keys("'a");
        $ctx->delay(100);
        $ctx->snapshot('after_quote_a');
    },
}
__DATA__
    indented line
normal line
