{
    desc => q{`a jumps to the exact mark position (line and column)},
    description => <<'END_DESC',
# mark_jump_backtick, `a jumps to the exact mark position (line and column)

Macro: `xt/visual/macros/marks/mark_jump_backtick`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## after_move_right.png

- Keys    : 4l

## after_set_mark.png

- Keys    : ma

## after_move_down.png

- Keys    : jjj

## after_backtick_a.png

- Keys    : `a


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('4l');
        $ctx->delay(100);
        $ctx->snapshot('after_move_right');
        $ctx->delay(100);
        $ctx->keys('ma');
        $ctx->delay(100);
        $ctx->snapshot('after_set_mark');
        $ctx->delay(100);
        $ctx->keys('jjj');
        $ctx->delay(100);
        $ctx->snapshot('after_move_down');
        $ctx->delay(100);
        $ctx->keys('`a');
        $ctx->delay(100);
        $ctx->snapshot('after_backtick_a');
    },
}
__DATA__
line one
line two
line three
line four
line five
