{
    desc => ': then Return (empty command) returns to normal mode',
    description => <<'END_DESC',
# command_empty_return, : then Return (empty command) returns to normal mode

Macro: `xt/visual/macros/modes/command_empty_return`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## after_empty_return.png

- Keys    : : `Enter`


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys(':\n');
        $ctx->delay(100);
        $ctx->snapshot('after_empty_return');
    },
}
__DATA__
hello world
