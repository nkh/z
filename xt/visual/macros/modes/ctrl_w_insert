{
    desc => q{Ctrl-w deletes word backward in insert mode},
    description => <<'END_DESC',
# ctrl_w_insert, Ctrl-w deletes word backward in insert mode

Macro: `xt/visual/macros/modes/ctrl_w_insert`

## initial.png

- Cursor: 1, 0
- Line 1  : hello

## after_typing.png


## after_ctrl_w.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->key('A');
        $ctx->type(' beautiful world');
        $ctx->delay(100);
        $ctx->snapshot('after_typing');
        $ctx->delay(100);
        $ctx->key('Control-w');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_ctrl_w');
    },
}
__DATA__
hello
