{
    desc => 'Ctrl-G shows file info in the status bar',
    description => <<'END_DESC',
# ctrl_g_file_info, Ctrl-G shows file info in the status bar

Macro: `xt/visual/macros/modes/ctrl_g_file_info`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world
- Line 2  : foo bar baz

## after_ctrl_g.png

- Keys    : Control-g


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Control-g');
        $ctx->delay(200);
        $ctx->snapshot('after_ctrl_g');
    },
}
__DATA__
hello world
foo bar baz
