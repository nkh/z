{
    desc => ': then Escape returns to normal mode',
    description => <<'END_DESC',
# command_to_normal, : then Escape returns to normal mode

Macro: `xt/visual/macros/modes/command_to_normal`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## after_escape.png

- Keys    : :\e


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys(':\e');
        $ctx->delay(100);
        $ctx->snapshot('after_escape');
    },
}
__DATA__
hello world
