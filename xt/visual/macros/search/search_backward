{
    desc => q{?pattern searches backward for a pattern},
    description => <<'END_DESC',
# search_backward, ?pattern searches backward for a pattern

Macro: `xt/visual/macros/search/search_backward`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world
- Line 2  : foo bar hello baz

## after_move_down.png

- Keys    : j

## after_search_backward.png

- Keys    : ?hello `Enter`


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('j');
        $ctx->delay(100);
        $ctx->snapshot('after_move_down');
        $ctx->delay(100);
        $ctx->keys('?hello\n');
        $ctx->delay(300);
        $ctx->snapshot('after_search_backward');
    },
}
__DATA__
hello world
foo bar hello baz
