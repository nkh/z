{
    desc => q{n navigates through regex matches in order},
    description => <<'END_DESC',
# search_regex_n_navigation, n navigates through regex matches in order

Macro: `xt/visual/macros/search/search_regex_n_navigation`

## initial.png

- Cursor: 1, 0
- Line 1  : move max moon

## after_regex_search.png

- Keys    : /m. `Enter`

## after_first_n.png

- Keys    : n

## after_second_n.png

- Keys    : n


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('/m.\n');
        $ctx->delay(300);
        $ctx->snapshot('after_regex_search');
        $ctx->delay(100);
        $ctx->keys('n');
        $ctx->delay(200);
        $ctx->snapshot('after_first_n');
        $ctx->delay(100);
        $ctx->keys('n');
        $ctx->delay(200);
        $ctx->snapshot('after_second_n');
    },
}
__DATA__
move max moon
