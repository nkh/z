{
    desc => q{// repeats the last search},
    description => <<'END_DESC',
# search_empty_pattern, // repeats the last search

Macro: `xt/visual/macros/search/search_empty_pattern`

## initial.png

- Cursor: 1, 0
- Line 1  : foo bar foo baz foo

## after_first_search.png

- Keys    : /bar `Enter`

## after_double_slash.png

- Keys    : / `Enter`


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('/bar\n');
        $ctx->delay(300);
        $ctx->snapshot('after_first_search');
        $ctx->delay(100);
        $ctx->keys('/\n');
        $ctx->delay(300);
        $ctx->snapshot('after_double_slash');
    },
}
__DATA__
foo bar foo baz foo
