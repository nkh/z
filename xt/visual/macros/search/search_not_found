{
    desc => q{Searching for a non-existent pattern shows an error},
    description => <<'END_DESC',
# search_not_found, Searching for a non-existent pattern shows an error

Macro: `xt/visual/macros/search/search_not_found`

## initial.png

- Cursor: 1, 0
- Line 1  : foo bar baz

## after_not_found.png

- Keys    : /zzznotfound `Enter`


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('/zzznotfound\n');
        $ctx->delay(300);
        $ctx->snapshot('after_not_found');
    },
}
__DATA__
foo bar baz
