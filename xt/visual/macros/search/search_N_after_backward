{
    desc => q{N after backward search goes forward},
    description => <<'END_DESC',
# search_N_after_backward, N after backward search goes forward

Macro: `xt/visual/macros/search/search_N_after_backward`

## initial.png

- Cursor: 1, 0
- Line 1  : alpha beta alpha gamma alpha

## after_move_to_end.png

- Keys    : $

## after_backward_search.png

- Keys    : ?alpha `Enter`

## after_N_forward.png

- Keys    : N


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$');
        $ctx->delay(100);
        $ctx->snapshot('after_move_to_end');
        $ctx->delay(100);
        $ctx->keys('?alpha\n');
        $ctx->delay(300);
        $ctx->snapshot('after_backward_search');
        $ctx->delay(100);
        $ctx->keys('N');
        $ctx->delay(200);
        $ctx->snapshot('after_N_forward');
    },
}
__DATA__
alpha beta alpha gamma alpha
