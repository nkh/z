{
    desc => q{/ with regex metacharacters matches patterns},
    description => <<'END_DESC',
# search_regex, / with regex metacharacters matches patterns

Macro: `xt/visual/macros/search/search_regex`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world
- Line 2  : hxllo world
- Line 3  : hlllo world

## after_regex_search.png

- Keys    : /h.llo `Enter`


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('/h.llo\n');
        $ctx->delay(300);
        $ctx->snapshot('after_regex_search');
    },
}
__DATA__
hello world
hxllo world
hlllo world
