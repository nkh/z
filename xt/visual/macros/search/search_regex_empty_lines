{
    desc => q{/^$ regex matches empty lines correctly},
    description => <<'END_DESC',
# search_regex_empty_lines, /^$ regex matches empty lines correctly

Macro: `xt/visual/macros/search/search_regex_empty_lines`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : 
- Line 3  : line two
- Line 4  : 
- Line 5  : line three

## after_search_empty.png

- Keys    : /^$ `Enter`

## after_first_n.png

- Keys    : n

## after_second_n.png

- Keys    : n

## after_fourth_n.png

- Keys    : nn


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(200);
        $ctx->snapshot('initial');
        $ctx->delay(200);
        $ctx->keys('/^$\n');
        $ctx->delay(500);
        $ctx->snapshot('after_search_empty');
        $ctx->delay(100);
        $ctx->keys('n');
        $ctx->delay(300);
        $ctx->snapshot('after_first_n');
        $ctx->delay(100);
        $ctx->keys('n');
        $ctx->delay(300);
        $ctx->snapshot('after_second_n');
        $ctx->delay(100);
        $ctx->keys('nn');
        $ctx->delay(300);
        $ctx->snapshot('after_fourth_n');
    },
}
__DATA__
line one

line two

line three
