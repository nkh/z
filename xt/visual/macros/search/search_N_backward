{
    desc => q{N repeats search in opposite direction (backward after forward)},
    description => <<'END_DESC',
# search_N_backward, N repeats search in opposite direction (backward after forward)

Macro: `xt/visual/macros/search/search_N_backward`

## initial.png

- Cursor: 1, 0
- Line 1  : alpha beta alpha gamma alpha

## after_forward_search.png

- Keys    : /alpha `Enter`

## after_N.png

- Keys    : N


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('/alpha\n');
        $ctx->delay(300);
        $ctx->snapshot('after_forward_search');
        $ctx->delay(100);
        $ctx->keys('N');
        $ctx->delay(200);
        $ctx->snapshot('after_N');
    },
}
__DATA__
alpha beta alpha gamma alpha
