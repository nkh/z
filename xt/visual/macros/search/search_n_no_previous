{
    desc => q{n with no previous search shows error},
    description => <<'END_DESC',
# search_n_no_previous, n with no previous search shows error

Macro: `xt/visual/macros/search/search_n_no_previous`

## initial.png

- Cursor: 1, 0
- Line 1  : foo bar baz

## after_n_no_prev.png

- Keys    : n


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('n');
        $ctx->delay(200);
        $ctx->snapshot('after_n_no_prev');
    },
}
__DATA__
foo bar baz
