{
    desc => q{# searches backward for the word under cursor},
    description => <<'END_DESC',
# search_word_hash, # searches backward for the word under cursor

Macro: `xt/visual/macros/search/search_word_hash`

## initial.png

- Cursor: 1, 0
- Line 1  : foo bar foo baz foo

## after_move_to_last.png

- Keys    : $hh

## after_hash.png

- Keys    : #


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$hh');
        $ctx->delay(100);
        $ctx->snapshot('after_move_to_last');
        $ctx->delay(100);
        $ctx->keys('#');
        $ctx->delay(300);
        $ctx->snapshot('after_hash');
    },
}
__DATA__
foo bar foo baz foo
