{
    desc => q{Empty / + Return with no previous search shows error},
    description => <<'END_DESC',
# search_empty_return, Empty / + Return with no previous search shows error

Macro: `xt/visual/macros/search/search_empty_return`

## initial.png

- Cursor: 1, 0
- Line 1  : foo bar baz

## after_empty_return.png

- Keys    : / `Enter`


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('/\n');
        $ctx->delay(300);
        $ctx->snapshot('after_empty_return');
    },
}
__DATA__
foo bar baz
