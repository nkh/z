{
    desc => q{u undoes mixed operations (x and dd)},
    description => <<'END_DESC',
# undo_mixed_x_dd, u undoes mixed operations (x and dd)

Macro: `xt/visual/macros/undo_redo/undo_mixed_x_dd`

## initial.png

- Cursor: 1, 0
- Line 1  : alpha
- Line 2  : beta
- Line 3  : gamma

## after_mixed_changes.png

- Keys    : x jdd

## after_undo_dd.png

- Keys    : u

## after_undo_all.png

- Keys    : u


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('x');
        $ctx->delay(50);
        $ctx->keys('jdd');
        $ctx->delay(100);
        $ctx->snapshot('after_mixed_changes');
        $ctx->delay(100);
        $ctx->keys('u');
        $ctx->delay(100);
        $ctx->snapshot('after_undo_dd');
        $ctx->delay(100);
        $ctx->keys('u');
        $ctx->delay(100);
        $ctx->snapshot('after_undo_all');
    },
}
__DATA__
alpha
beta
gamma
