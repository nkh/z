{
    desc => q{u undoes a 2dd (counted line delete)},
    description => <<'END_DESC',
# undo_2dd, u undoes a 2dd (counted line delete)

Macro: `xt/visual/macros/undo_redo/undo_2dd`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three

## after_2dd.png

- Keys    : 2dd

## after_undo.png

- Keys    : u


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('2dd');
        $ctx->delay(100);
        $ctx->snapshot('after_2dd');
        $ctx->delay(100);
        $ctx->keys('u');
        $ctx->delay(100);
        $ctx->snapshot('after_undo');
    },
}
__DATA__
line one
line two
line three
