{
    desc => q{u restores cursor position after dd},
    description => <<'END_DESC',
# undo_cursor_position, u restores cursor position after dd

Macro: `xt/visual/macros/undo_redo/undo_cursor_position`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four

## after_dd.png

- Keys    : dd

## after_undo_cursor_restored.png

- Keys    : u


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->keys('jl');
        $ctx->delay(50);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('dd');
        $ctx->delay(100);
        $ctx->snapshot('after_dd');
        $ctx->delay(100);
        $ctx->keys('u');
        $ctx->delay(100);
        $ctx->snapshot('after_undo_cursor_restored');
    },
}
__DATA__
line one
line two
line three
line four
