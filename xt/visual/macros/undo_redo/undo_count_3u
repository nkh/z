{
    desc => q{3u undoes 3 changes at once},
    description => <<'END_DESC',
# undo_count_3u, 3u undoes 3 changes at once

Macro: `xt/visual/macros/undo_redo/undo_count_3u`

## initial.png

- Cursor: 1, 0
- Line 1  : one
- Line 2  : two
- Line 3  : three

## after_3dd.png

- Keys    : dd dd dd

## after_3u.png

- Keys    : 3u


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('dd');
        $ctx->delay(50);
        $ctx->keys('dd');
        $ctx->delay(50);
        $ctx->keys('dd');
        $ctx->delay(100);
        $ctx->snapshot('after_3dd');
        $ctx->delay(100);
        $ctx->keys('3u');
        $ctx->delay(100);
        $ctx->snapshot('after_3u');
    },
}
__DATA__
one
two
three
