{
    desc => q{multiple sequential undo operations restore original buffer},
    description => <<'END_DESC',
# undo_multiple, multiple sequential undo operations restore original buffer

Macro: `xt/visual/macros/undo_redo/undo_multiple`

## initial.png

- Cursor: 1, 0
- Line 1  : aaa
- Line 2  : bbb
- Line 3  : ccc
- Line 4  : ddd

## after_changes.png

- Keys    : xx jdd

## after_partial_undo.png

- Keys    : uu

## after_all_undo.png

- Keys    : u


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('xx');
        $ctx->delay(50);
        $ctx->keys('jdd');
        $ctx->delay(100);
        $ctx->snapshot('after_changes');
        $ctx->delay(100);
        $ctx->keys('uu');
        $ctx->delay(100);
        $ctx->snapshot('after_partial_undo');
        $ctx->delay(100);
        $ctx->keys('u');
        $ctx->delay(100);
        $ctx->snapshot('after_all_undo');
    },
}
__DATA__
aaa
bbb
ccc
ddd
