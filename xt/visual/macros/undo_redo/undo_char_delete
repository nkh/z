{
    desc => q{u undoes a character delete (x)},
    description => <<'END_DESC',
# undo_char_delete, u undoes a character delete (x)

Macro: `xt/visual/macros/undo_redo/undo_char_delete`

## initial.png

- Cursor: 1, 0
- Line 1  : hello

## after_delete.png

- Keys    : x

## after_undo.png

- Keys    : u


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('x');
        $ctx->delay(100);
        $ctx->snapshot('after_delete');
        $ctx->delay(100);
        $ctx->keys('u');
        $ctx->delay(100);
        $ctx->snapshot('after_undo');
    },
}
__DATA__
hello
