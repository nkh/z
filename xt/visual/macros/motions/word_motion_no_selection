{
    desc => 'w/b/e word motions leave no visual selection in normal mode',
    description => <<'END_DESC',
# word_motion_no_selection, w/b/e word motions leave no visual selection in normal mode

Macro: `xt/visual/macros/motions/word_motion_no_selection`

## initial.png

- Cursor: 1, 0
- Line 1  : one two three four five
- Line 2  : six seven eight nine ten

## after_w.png

- Keys    : w

## after_bbe.png

- Keys    : bbe


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('w');
        $ctx->delay(100);
        $ctx->snapshot('after_w');
        $ctx->delay(100);
        $ctx->keys('bbe');
        $ctx->delay(100);
        $ctx->snapshot('after_bbe');
    },
}
__DATA__
one two three four five
six seven eight nine ten
