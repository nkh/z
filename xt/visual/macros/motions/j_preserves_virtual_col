{
    desc => q{j preserves virtual column across short lines},
    description => <<'END_DESC',
# j_preserves_virtual_col, j preserves virtual column across short lines

Macro: `xt/visual/macros/motions/j_preserves_virtual_col`

## initial.png

- Cursor: 1, 0
- Line 1  : a very long line of text here
- Line 2  : xy
- Line 3  : another very long line of text here

## at_eol.png

- Keys    : $

## on_short.png

- Keys    : j


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$');
        $ctx->delay(100);
        $ctx->snapshot('at_eol');
        $ctx->delay(100);
        $ctx->keys('j');
        $ctx->delay(100);
        $ctx->snapshot('on_short');
    },
}
__DATA__
a very long line of text here
xy
another very long line of text here
