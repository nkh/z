{
    desc => q{Virtual column preserved across j/k on short lines},
    description => <<'END_DESC',
# virtual_column_jk, Virtual column preserved across j/k on short lines

Macro: `xt/visual/macros/motions/virtual_column_jk`

## initial.png

- Cursor: 1, 0
- Line 1  : abcdefghijklmnopqrstuvwxyz
- Line 2  : ab
- Line 3  : abcdefghijklmnopqrstuvwxyz

## at_eol.png

- Keys    : $

## on_short.png

- Keys    : j

## restored.png

- Keys    : j


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$');
        $ctx->delay(100);
        $ctx->snapshot('at_eol');
        $ctx->delay(100);
        $ctx->keys('j');
        $ctx->delay(100);
        $ctx->snapshot('on_short');
        $ctx->delay(100);
        $ctx->keys('j');
        $ctx->delay(100);
        $ctx->snapshot('restored');
    },
}
__DATA__
abcdefghijklmnopqrstuvwxyz
ab
abcdefghijklmnopqrstuvwxyz
