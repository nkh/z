{
    desc => ':set nocursorl is abbreviation for :set nocursorline',
    description => <<'END_DESC',
# set_nocursorline_abbrev, :set nocursorl is abbreviation for :set nocursorline

Macro: `xt/visual/macros/settings/set_nocursorline_abbrev`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## with_highlight.png

- Keys    : :set`Space` cursorline

## after_nocursorl.png

- Keys    : :set`Space` nocursorl


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->ex('set cursorline');
        $ctx->delay(200);
        $ctx->snapshot('with_highlight');
        $ctx->delay(100);
        $ctx->ex('set nocursorl');
        $ctx->delay(200);
        $ctx->snapshot('after_nocursorl');
    },
}
__DATA__
line one
line two
line three
line four
line five
