{
    desc => ':set nu is abbreviation for :set number',
    description => <<'END_DESC',
Macro: `xt/visual/macros/set_number_abbrev`

Tests that `:set nu` is accepted as an abbreviation for `:set number` and
displays line numbers in the left gutter.

Ex: `:set nu`

- **set_number_abbrev_initial.png**: No line numbers visible.
- **set_number_abbrev_after_set.png**: Line numbers (1-5) displayed in the left margin.
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set nu');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
line one
line two
line three
line four
line five
