{
    desc => ':set ft=invalid shows error for unknown filetype',
    description => <<'END_DESC',
# set_filetype_error, :set ft=invalid shows error for unknown filetype

Macro: `xt/visual/macros/settings/set_filetype_error`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## after_set.png

- Keys    : :set`Space` filetype=nonexistent_lang


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set filetype=nonexistent_lang');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
hello world
