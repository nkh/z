{
    desc => ':set ft=invalid shows error for unknown filetype',
    description => <<'END_DESC',
Macro: `xt/visual/macros/set_filetype_error`

Tests that `:set filetype=` with an unknown language displays an error
message in the status bar.

Ex: `:set filetype=nonexistent_lang`

- **set_filetype_error_initial.png**: Normal mode, plain text.
- **set_filetype_error_after_set.png**: Error message displayed for unknown filetype.
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set filetype=nonexistent_lang');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
hello world
