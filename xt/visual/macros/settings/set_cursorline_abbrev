{
    desc => ':set cursorl is abbreviation for :set cursorline',
    description => <<'END_DESC',
# set_cursorline_abbrev, :set cursorl is abbreviation for :set cursorline

Macro: `xt/visual/macros/settings/set_cursorline_abbrev`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## after_set.png

- Keys    : :set`Space` cursorl


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set cursorl');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
line one
line two
line three
line four
line five
