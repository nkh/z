{
    desc => ':set nonumber hides line numbers',
    description => <<'END_DESC',
# set_nonumber, :set nonumber hides line numbers

Macro: `xt/visual/macros/settings/set_nonumber`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## with_numbers.png

- Keys    : :set`Space` number

## after_nonumber.png

- Keys    : :set`Space` nonumber


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->ex('set number');
        $ctx->delay(100);
        $ctx->snapshot('with_numbers');
        $ctx->delay(100);
        $ctx->ex('set nonumber');
        $ctx->delay(200);
        $ctx->snapshot('after_nonumber');
    },
}
__DATA__
line one
line two
line three
line four
line five
