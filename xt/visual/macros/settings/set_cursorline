{
    desc => ':set cursorline highlights the current line',
    description => <<'END_DESC',
# set_cursorline, :set cursorline highlights the current line

Macro: `xt/visual/macros/settings/set_cursorline`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## after_set.png

- Keys    : :set`Space` cursorline


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set cursorline');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
line one
line two
line three
line four
line five
