{
    desc => ':set theme=nonexistent shows an error for unknown theme names',
    description => <<'END_DESC',
# set_theme_error, :set theme=nonexistent shows an error for unknown theme names

Macro: `xt/visual/macros/settings/set_theme_error`

## initial.png

- Cursor: 1, 0
- Line 1  : # Theme switch test
- Line 2  : Hello, World!
- Line 3  : my $x = 42;
- Line 4  : print "dark mode\n";

## dark_before.png


## message.png

- Keys    : :set`Space` theme=nonexistent


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->editor->set_theme('dark');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('dark_before');
        $ctx->delay(100);
        $ctx->ex('set theme=nonexistent');
        $ctx->delay(100);
        $ctx->snapshot('message');
    },
}
__DATA__
# Theme switch test
Hello, World!
my $x = 42;
print "dark mode\n";
