{
    desc => ':set ft= abbreviation sets filetype for syntax highlighting',
    description => <<'END_DESC',
# set_filetype_abbrev, :set ft= abbreviation sets filetype for syntax highlighting

Macro: `xt/visual/macros/settings/set_filetype_abbrev`

## initial.png

- Cursor: 1, 0
- Line 1  : package MyApp;
- Line 2  : use strict;
- Line 3  : use warnings;
- Line 4  : 
- Line 5  : sub new {
- Line 6  :     my $class = shift;
- Line 7  :     return bless {}, $class;
- Line 8  : }
- Line 9  : 
- Line 10  : 1;

## after_set.png

- Keys    : :set`Space` ft=perl


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set ft=perl');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
package MyApp;
use strict;
use warnings;

sub new {
    my $class = shift;
    return bless {}, $class;
}

1;
