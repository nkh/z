{
    desc => ':set tabstop=invalid shows error for non-numeric value',
    description => <<'END_DESC',
# set_tabstop_error, :set tabstop=invalid shows error for non-numeric value

Macro: `xt/visual/macros/settings/set_tabstop_error`

## initial.png

- Cursor: 1, 0
- Line 1  : some text here

## after_set.png

- Keys    : :set`Space` tabstop=abc


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set tabstop=abc');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
some text here
