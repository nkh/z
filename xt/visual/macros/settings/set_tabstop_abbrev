{
    desc => q{:set ts=8 abbreviation sets tabstop width},
    description => <<'END_DESC',
# set_tabstop_abbrev, :set ts=8 abbreviation sets tabstop width

Macro: `xt/visual/macros/settings/set_tabstop_abbrev`

## initial.png

- Cursor: 1, 0
- Line 1  : 	indented line one
- Line 2  : 	indented line two
- Line 3  : 	normal line three

## after_set.png

- Keys    : :set`Space` ts=8


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set ts=8');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
	indented line one
	indented line two
	normal line three
