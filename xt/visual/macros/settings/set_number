{
    desc => ':set number shows line numbers in the gutter',
    description => <<'END_DESC',
# set_number, :set number shows line numbers in the gutter

Macro: `xt/visual/macros/settings/set_number`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four
- Line 5  : line five

## after_set.png

- Keys    : :set`Space` number


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set number');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
line one
line two
line three
line four
line five
