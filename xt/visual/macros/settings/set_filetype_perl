{
    desc => ':set filetype=perl enables Perl syntax highlighting',
    description => <<'END_DESC',
Macro: `xt/visual/macros/set_filetype_perl`

Tests that `:set filetype=perl` enables Perl syntax highlighting, coloring
keywords (`package`, `use`, `sub`, `my`, `return`), strings, and comments.

Ex: `:set filetype=perl`

- **set_filetype_perl_initial.png**: Plain text, no syntax highlighting.
- **set_filetype_perl_after_set.png**: Perl syntax highlighting active; keywords, strings, and comments are colored distinctly.
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set filetype=perl');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
package MyApp;
use strict;
use warnings;

sub new {
    my $class = shift;
    return bless {}, $class;
}

sub run {
    my $self = shift;
    print "running\n";
}

1;
