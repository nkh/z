{
    desc => ':set filetype=perl enables Perl syntax highlighting',
    description => <<'END_DESC',
# set_filetype_perl, :set filetype=perl enables Perl syntax highlighting

Macro: `xt/visual/macros/settings/set_filetype_perl`

## initial.png

- Cursor: 1, 0
- Line 1  : package MyApp;
- Line 2  : use strict;
- Line 3  : use warnings;
- Line 4  : 
- Line 5  : sub new {
- Line 6  :     my $class = shift;
- Line 7  :     return bless {}, $class;
- Line 8  : }
- Line 9  : 
- Line 10  : sub run {
- Line 11  :     my $self = shift;
- Line 12  :     print "running\n";
- Line 13  : }
- Line 14  : 
- Line 15  : 1;

## after_set.png

- Keys    : :set`Space` filetype=perl


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set filetype=perl');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
package MyApp;
use strict;
use warnings;

sub new {
    my $class = shift;
    return bless {}, $class;
}

sub run {
    my $self = shift;
    print "running\n";
}

1;
