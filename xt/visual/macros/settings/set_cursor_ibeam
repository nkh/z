{
    desc => ':set cursor=ibeam changes cursor shape to I-beam',
    description => <<'END_DESC',
# set_cursor_ibeam, :set cursor=ibeam changes cursor shape to I-beam

Macro: `xt/visual/macros/settings/set_cursor_ibeam`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three

## after_set.png

- Keys    : :set`Space` cursor=ibeam


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('set cursor=ibeam');
        $ctx->delay(200);
        $ctx->snapshot('after_set');
    },
}
__DATA__
line one
line two
line three
