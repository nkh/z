{
    desc => ':%s/pat/repl/g substitutes all occurrences in entire file',
    description => <<'END_DESC',
# ex_substitute_global, :%s/pat/repl/g substitutes all occurrences in entire file

Macro: `xt/visual/macros/ex_commands/ex_substitute_global`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world hello
- Line 2  : foo bar hello baz

## after_global_substitute.png

- Keys    : :%s/hello/HI/g


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('%s/hello/HI/g');
        $ctx->delay(200);
        $ctx->snapshot('after_global_substitute');
    },
}
__DATA__
hello world hello
foo bar hello baz
