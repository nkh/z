{
    desc => ':s/pat/repl/g substitutes all occurrences on current line',
    description => <<'END_DESC',
# ex_substitute_all, :s/pat/repl/g substitutes all occurrences on current line

Macro: `xt/visual/macros/ex_commands/ex_substitute_all`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world hello again

## after_substitute_all.png

- Keys    : :s/hello/HI/g


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->ex('s/hello/HI/g');
        $ctx->delay(200);
        $ctx->snapshot('after_substitute_all');
    },
}
__DATA__
hello world hello again
