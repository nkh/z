{
    desc => 'Visual block unindent with << unindents block lines',
    description => <<'END_DESC',
Action: Indent lines first, then Ctrl-V select and unindent

-1_initial.png (initial state):
- Cursor at line 1, col 0

Sub-commands: keys('>>'), keys('jj>>'), keys('kkControl-vjj'), keys('<<')

-2_after.png (after action):
- All 3 lines unindented back
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('>>');
        $ctx->delay(50);
        $ctx->keys('jj>>');
        $ctx->delay(50);
        $ctx->keys('kkControl-vjj');
        $ctx->delay(50);
        $ctx->keys('<<');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
abcd
efgh
ijkl
mnop
