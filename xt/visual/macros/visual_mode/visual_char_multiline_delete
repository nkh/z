{
    desc => q{Visual char multiline delete},
    description => <<'END_DESC',
# visual_char_multiline_delete, Visual char multiline delete

Macro: `xt/visual/macros/visual_mode/visual_char_multiline_delete`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world this is a test
- Line 2  : second line of text here

## after.png

- Keys    : 5vj$ld


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('5vj$ld');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line of text here
