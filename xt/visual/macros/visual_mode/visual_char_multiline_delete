{
    desc => q{Visual char multiline delete},
    description => <<'END_DESC',
Action: v selects across multiple lines, d deletes

-1_initial.png (initial state):
- Line 1: `hello world this is a test`
- Cursor at line 1, col 5 (on ` `)

Sub-commands: keys('5vj$ld')

-2_after.png (after action):
- Selected text spanning 2 lines deleted
- Line 1 truncated, line 2 content changed
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('5vj$ld');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line of text here
