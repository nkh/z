{
    desc => q{Visual line toggle case},
    description => <<'END_DESC',
# visual_line_toggle_case, Visual line toggle case

Macro: `xt/visual/macros/visual_mode/visual_line_toggle_case`

## initial.png

- Cursor: 1, 0
- Line 1  : Hello World
- Line 2  : This is line two
- Line 3  : third line unchanged

## after.png

- Keys    : Vj~


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Vj~');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
Hello World
This is line two
third line unchanged
