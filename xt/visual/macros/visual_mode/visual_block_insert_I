{
    desc => q{Visual block insert I},
    description => <<'END_DESC',
# visual_block_insert_I, Visual block insert I

Macro: `xt/visual/macros/visual_mode/visual_block_insert_I`

## initial.png

- Cursor: 1, 0
- Line 1  : abcd
- Line 2  : efgh
- Line 3  : ijkl
- Line 4  : mnop

## after.png

- Keys    : Control-vjj


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Control-vjj');
        $ctx->delay(50);
        $ctx->key('I');
        $ctx->type('>');
        $ctx->key('Escape');
        $ctx->delay(200);
        $ctx->snapshot('after');
    },
}
__DATA__
abcd
efgh
ijkl
mnop
