{
    desc => q{Visual yank then paste},
    description => <<'END_DESC',
Action: v selects text, y yanks, then p pastes

-1_initial.png (initial state):
- Cursor at line 1, col 0

Sub-commands: keys('vell yp')

-2_after.png (after action):
- "hello" pasted after the selection
- Content: "hellohello world this is a test"
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('vell yp');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}__DATA__
hello world this is a test
second line of text here
third line for more context
