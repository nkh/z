{
    desc => q{Visual swap ends o},
    description => <<'END_DESC',
# visual_swap_ends, Visual swap ends o

Macro: `xt/visual/macros/visual_mode/visual_swap_ends`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world this is a test

## after.png

- Keys    : v$od


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('v$od');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
