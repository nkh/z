{
    desc => q{Visual swap ends o},
    description => <<'END_DESC',
Action: v selects chars, o swaps cursor/anchor, d deletes

-1_initial.png (initial state):
- Cursor at line 1, col 0

Sub-commands: keys('v$od')

-2_after.png (after action):
- All characters except the last deleted (backward selection deleted)
- Line 1: "d" (only the last char remains)
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('v$od');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
