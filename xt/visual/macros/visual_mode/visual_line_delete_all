{
    desc => q{Visual line delete all lines},
    description => <<'END_DESC',
# visual_line_delete_all, Visual line delete all lines

Macro: `xt/visual/macros/visual_mode/visual_line_delete_all`

## initial.png

- Cursor: 1, 0
- Line 1  : first line
- Line 2  : second line
- Line 3  : third line

## after.png

- Keys    : VGd


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('VGd');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
first line
second line
third line
