{
    desc => q{Visual line delete},
    description => <<'END_DESC',
Action: V selects lines, d deletes them

-1_initial.png (initial state):
- Cursor at line 1

Sub-commands: keys('Vjd')

-2_after.png (after action):
- First 2 lines deleted
- Buffer starts with "line three"
- Cursor at line 1
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Vjd');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
line one
line two
line three
line four
