{
    desc => q{Visual delete backward},
    description => <<'END_DESC',
# visual_delete_backward, Visual delete backward

Macro: `xt/visual/macros/visual_mode/visual_delete_backward`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world this is a test
- Line 2  : second line of text here

## after.png

- Keys    : 5lvhhd


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('5lvhhd');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line of text here
