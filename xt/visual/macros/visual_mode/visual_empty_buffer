{
    desc => q{Empty buffer},
    description => <<'END_DESC',
# visual_empty_buffer, Empty buffer

Macro: `xt/visual/macros/visual_mode/visual_empty_buffer`


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot();
    },
}
