{
    desc => q{Single line},
    description => <<'END_DESC',
# visual_single_line, Single line

Macro: `xt/visual/macros/visual_mode/visual_single_line`

## initial.png

- Cursor: 1, 0
- Line 1  : one line of text


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot();
    },
}
__DATA__
one line of text
