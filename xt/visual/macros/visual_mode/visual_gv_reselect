{
    desc => q{Visual gv re-select},
    description => <<'END_DESC',
Action: v selects, d deletes, gv re-selects the deleted region

-1_initial.png (initial state):
- Cursor at line 1, col 0

Sub-commands: keys('vllldgv')

-2_after.png (after action):
- "hell" deleted: "o world this is a test"
- gv re-selected the region (selection visible on the remaining text)
- Mode label shows "-- VISUAL --"
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('vllldgv');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line of text here
third line for more context
