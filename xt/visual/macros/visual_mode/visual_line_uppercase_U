{
    desc => q{Visual line uppercase U},
    description => <<'END_DESC',
# visual_line_uppercase_U, Visual line uppercase U

Macro: `xt/visual/macros/visual_mode/visual_line_uppercase_U`

## initial.png

- Cursor: 1, 0
- Line 1  : Hello World
- Line 2  : This is line two
- Line 3  : third line unchanged

## after.png

- Keys    : VjU


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('VjU');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
Hello World
This is line two
third line unchanged
