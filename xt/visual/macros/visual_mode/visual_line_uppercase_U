{
    desc => q{Visual line uppercase U},
    description => <<'END_DESC',
Action: V selects lines, U uppercases entire selection

-1_initial.png (initial state):
- Cursor at line 1

Sub-commands: keys('VjU')

-2_after.png (after action):
- Lines 1-2 uppercased: "HELLO WORLD", "THIS IS LINE TWO"
- Line 3 unchanged
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('VjU');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}__DATA__
Hello World
This is line two
third line unchanged
