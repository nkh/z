{
    desc => q{Visual unindent left <<},
    description => <<'END_DESC',
Action: V selects lines, << unindents them

-1_initial.png (initial state):
- Lines 1-2 indented, cursor at line 1

Sub-commands: keys('Vj<<')

-2_after.png (after action):
- First 2 lines unindented
- Line 3 unchanged
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Vj<<');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}__DATA__
    indented line one
    indented line two
not indented line three
