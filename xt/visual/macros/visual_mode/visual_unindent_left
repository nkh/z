{
    desc => q{Visual unindent left <<},
    description => <<'END_DESC',
# visual_unindent_left, Visual unindent left <<

Macro: `xt/visual/macros/visual_mode/visual_unindent_left`

## initial.png

- Cursor: 1, 0
- Line 1  :     indented line one
- Line 2  :     indented line two
- Line 3  : not indented line three

## after.png

- Keys    : Vj<<


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Vj<<');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
    indented line one
    indented line two
not indented line three
