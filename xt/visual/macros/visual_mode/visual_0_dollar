{
    desc => q{Visual 0/$ motions},
    description => <<'END_DESC',
# visual_0_dollar, Visual 0/$ motions

Macro: `xt/visual/macros/visual_mode/visual_0_dollar`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world this is a test
- Line 2  : second line of text here

## after.png

- Keys    : 5lv0$


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('5lv0$');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line of text here
