{
    desc => q{Visual 0/$ motions},
    description => <<'END_DESC',
Action: v select, 0 to line start, $ to line end

-1_initial.png (initial state):
- Cursor at line 1, col 5 (on 'w')

Sub-commands: keys('5lv0$')

-2_after.png (after action):
- Entire line 1 selected (from col 0 to end)
- Mode label: "-- VISUAL --"
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('5lv0$');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line of text here
