{
    desc => 'Visual block lowercase u converts block to lowercase',
    description => <<'END_DESC',
# visual_block_lowercase, Visual block lowercase u converts block to lowercase

Macro: `xt/visual/macros/visual_mode/visual_block_lowercase`

## initial.png

- Cursor: 1, 0
- Line 1  : ABCD
- Line 2  : EFGH
- Line 3  : IJKL

## after.png

- Keys    : Control-vjjl


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Control-vjjl');
        $ctx->delay(50);
        $ctx->key('u');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
ABCD
EFGH
IJKL
