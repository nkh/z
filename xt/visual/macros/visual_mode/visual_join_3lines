{
    desc => 'Visual join 3 lines merges multiple lines',
    description => <<'END_DESC',
# visual_join_3lines, Visual join 3 lines merges multiple lines

Macro: `xt/visual/macros/visual_mode/visual_join_3lines`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four

## after.png

- Keys    : VjjJ


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('VjjJ');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
line one
line two
line three
line four
