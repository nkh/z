{
    desc => q{Delete line (dd)},
    description => <<'END_DESC',
# visual_delete_line, Delete line (dd)

Macro: `xt/visual/macros/visual_mode/visual_delete_line`

## initial.png

- Cursor: 1, 0
- Line 1  : #!/usr/bin/perl
- Line 2  : use strict;
- Line 3  : use warnings;
- Line 4  : print "Hello, World!\n";

## after.png

- Keys    : dd


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('dd');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
#!/usr/bin/perl
use strict;
use warnings;
print "Hello, World!\n";
