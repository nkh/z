{
    desc => q{Visual uppercase U},
    description => <<'END_DESC',
Action: v selects chars, U uppercases selection

-1_initial.png (initial state):
- Cursor at line 1, col 0

Sub-commands: keys('veeU')

-2_after.png (after action):
- First 2 words uppercased: `HELLO WORLD this is a test`
- Line 1: `HELLO WORLD this is a test`
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('veeU');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line of text here
third line for more context
