{
    desc => q{Visual block mode select and delete},
    description => <<'END_DESC',
Action: Ctrl-V selects block, d deletes columns

-1_initial.png (initial state):
- Cursor at line 1, col 0

Sub-commands: keys('Control-vjjld')

-2_after.png (after action):
- First 2 columns removed from each of first 3 lines
- Lines become "cd", "gh", "kl"
- Line 4 unchanged: "mnop"
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Control-vjjld');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
abcd
efgh
ijkl
mnop
