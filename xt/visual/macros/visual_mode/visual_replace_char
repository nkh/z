{
    desc => q{Replace mode (r)},
    description => <<'END_DESC',
# visual_replace_char, Replace mode (r)

Macro: `xt/visual/macros/visual_mode/visual_replace_char`

## initial.png

- Cursor: 1, 0
- Line 1  : #!/usr/bin/perl
- Line 2  : use strict;
- Line 3  : use warnings;
- Line 4  : print "Hello, World!\n";

## after.png

- Keys    : rx


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->editor->set_language('perl');
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('rx');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
#!/usr/bin/perl
use strict;
use warnings;
print "Hello, World!\n";
