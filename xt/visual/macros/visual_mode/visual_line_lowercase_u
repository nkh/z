{
    desc => q{Visual line lowercase u},
    description => <<'END_DESC',
Action: V selects lines, u lowercases entire selection

-1_initial.png (initial state):
- Cursor at line 1

Sub-commands: keys('Vju')

-2_after.png (after action):
- Lines 1-2 lowercased: "hello world", "this is line two"
- Line 3 unchanged
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Vju');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}__DATA__
Hello World
This is line two
third line unchanged
