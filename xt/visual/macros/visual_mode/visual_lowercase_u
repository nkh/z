{
    desc => q{Visual lowercase u},
    description => <<'END_DESC',
# visual_lowercase_u, Visual lowercase u

Macro: `xt/visual/macros/visual_mode/visual_lowercase_u`

## initial.png

- Cursor: 1, 0
- Line 1  : Hello World this is a test

## after.png

- Keys    : veeu


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('veeu');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
Hello World this is a test
