{
    desc => q{Visual lowercase u},
    description => <<'END_DESC',
Action: v selects chars, u lowercases selection

-1_initial.png (initial state):
- Line 1: `Hello World this is a test`
- Cursor at line 1, col 0

Sub-commands: keys('veeu')

-2_after.png (after action):
- First 2 words lowercased: `hello world this is a test`
- Line 1: `hello world this is a test`
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('veeu');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
Hello World this is a test
