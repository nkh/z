{
    desc => q{Visual char change},
    description => <<'END_DESC',
# visual_char_change, Visual char change

Macro: `xt/visual/macros/visual_mode/visual_char_change`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world this is a test
- Line 2  : second line here

## after.png

- Keys    : vlllc


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('vlllc');
        $ctx->delay(50);
        $ctx->type('XX');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line here
