{
    desc => q{Visual char change},
    description => <<'END_DESC',
Action: v selects chars, c changes (replaces with typed text)

-1_initial.png (initial state):
- Cursor at line 1, col 0

Sub-commands: keys('vlllc'), type('XX'), key('Escape')

-2_after.png (after action):
- First 4 characters replaced with "XX"
- Replaced text visible at cursor position
END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('vlllc');
        $ctx->delay(50);
        $ctx->type('XX');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
hello world this is a test
second line here
