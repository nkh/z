{
    desc => q{Visual line change},
    description => <<'END_DESC',
# visual_line_change, Visual line change

Macro: `xt/visual/macros/visual_mode/visual_line_change`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three
- Line 4  : line four

## after.png

- Keys    : Vjc


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Vjc');
        $ctx->delay(50);
        $ctx->type('REPLACED');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
line one
line two
line three
line four
