{
    desc => 'Visual block uppercase U converts block to uppercase',
    description => <<'END_DESC',
# visual_block_uppercase, Visual block uppercase U converts block to uppercase

Macro: `xt/visual/macros/visual_mode/visual_block_uppercase`

## initial.png

- Cursor: 1, 0
- Line 1  : abcd
- Line 2  : efgh
- Line 3  : ijkl

## after.png

- Keys    : Control-vjjl


END_DESC
    run => sub {
        my ($ctx) = @_;        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('Control-vjjl');
        $ctx->delay(50);
        $ctx->key('U');
        $ctx->delay(100);
        $ctx->snapshot('after');
    },
}
__DATA__
abcd
efgh
ijkl
