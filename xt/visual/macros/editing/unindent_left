{
    desc => '<< unindents current line left',
    description => <<'END_DESC',
# unindent_left, << unindents current line left

Macro: `xt/visual/macros/editing/unindent_left`

## initial.png

- Cursor: 1, 0
- Line 1  :         line one
- Line 2  :         line two
- Line 3  :         line three

## after_unindent.png

- Keys    : <<


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('<<');
        $ctx->delay(100);
        $ctx->snapshot('after_unindent');
    },
}
__DATA__
        line one
        line two
        line three
