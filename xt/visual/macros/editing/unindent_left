{
    desc => '<< unindents current line left',
    description => <<'END_DESC',
Macro: `xt/visual/macros/unindent_left`

Tests the `<<` operator which unindents the current line by one shift width.

**initial.png** — Three lines with 8-space indentation. Cursor at line 1.

Keys: `<<`

**after_unindent.png** — Line 1 indentation reduced by one shift width.
Lines 2 and 3 remain unchanged.
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('<<');
        $ctx->delay(100);
        $ctx->snapshot('after_unindent');
    },
}
__DATA__
        line one
        line two
        line three
