{
    desc => q{cc changes entire line},
    description => <<'END_DESC',
# change_line_cc, cc changes entire line

Macro: `xt/visual/macros/editing/change_line_cc`

## initial.png

- Cursor: 1, 0
- Line 1  : This is a longer line of text for testing changes.
- Line 2  : The quick brown fox jumps over the lazy dog.
- Line 3  : Another line here to fill the buffer.
- Line 4  : And yet another line for scrolling context.
- Line 5  : Final line of the test buffer content.

## after_cc.png

- Keys    : jcc


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('jcc');
        $ctx->delay(50);
        $ctx->type('new text');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_cc');
    },
}
__DATA__
This is a longer line of text for testing changes.
The quick brown fox jumps over the lazy dog.
Another line here to fill the buffer.
And yet another line for scrolling context.
Final line of the test buffer content.
