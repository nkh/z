{
    desc => q{s at end of line is a no-op},
    description => <<'END_DESC',
# substitute_at_eol, s at end of line is a no-op

Macro: `xt/visual/macros/editing/substitute_at_eol`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## cursor_at_eol.png

- Keys    : $

## after_s_at_eol.png

- Keys    : s


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('$');
        $ctx->delay(100);
        $ctx->snapshot('cursor_at_eol');
        $ctx->delay(100);
        $ctx->keys('s');
        $ctx->delay(200);
        $ctx->snapshot('after_s_at_eol');
    },
}
__DATA__
hello world
