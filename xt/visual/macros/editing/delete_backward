{
    desc => q{Backspace key in normal mode},
    description => <<'END_DESC',
# delete_backward, Backspace key in normal mode

Macro: `xt/visual/macros/editing/delete_backward`

## initial.png

- Cursor: 1, 0
- Line 1  : Hello world
- Line 2  : foo bar

## after_ll.png

- Keys    : ll

## after_first_bs.png


## after_second_bs.png


## at_col0.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('ll');
        $ctx->delay(100);
        $ctx->snapshot('after_ll');
        $ctx->delay(100);
        $ctx->key('BackSpace');
        $ctx->delay(100);
        $ctx->snapshot('after_first_bs');
        $ctx->delay(100);
        $ctx->key('BackSpace');
        $ctx->delay(100);
        $ctx->snapshot('after_second_bs');
        $ctx->delay(100);
        $ctx->key('BackSpace');
        $ctx->delay(100);
        $ctx->snapshot('at_col0');
    },
}
__DATA__
Hello world
foo bar
