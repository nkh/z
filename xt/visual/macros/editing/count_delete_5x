{
    desc => q{5x deletes 5 characters},
    description => <<'END_DESC',
# count_delete_5x, 5x deletes 5 characters

Macro: `xt/visual/macros/editing/count_delete_5x`

## initial.png

- Cursor: 1, 0
- Line 1  : This is a longer line of text for testing.
- Line 2  : The quick brown fox jumps over the lazy dog.
- Line 3  : Another line here to fill the buffer.
- Line 4  : And yet another line for scrolling context.
- Line 5  : Final line of the test buffer content.

## after_5x.png

- Keys    : 5x


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('5x');
        $ctx->delay(100);
        $ctx->snapshot('after_5x');
    },
}
__DATA__
This is a longer line of text for testing.
The quick brown fox jumps over the lazy dog.
Another line here to fill the buffer.
And yet another line for scrolling context.
Final line of the test buffer content.
