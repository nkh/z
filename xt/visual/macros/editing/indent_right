{
    desc => '>> indents current line right',
    description => <<'END_DESC',
# indent_right, >> indents current line right

Macro: `xt/visual/macros/editing/indent_right`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three

## after_indent_right.png

- Keys    : >>


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('>>');
        $ctx->delay(100);
        $ctx->snapshot('after_indent_right');
    },
}
__DATA__
line one
line two
line three
