{
    desc => '>> indents current line right',
    description => <<'END_DESC',
Macro: `xt/visual/macros/indent_right`

Tests the `>>` operator which indents the current line by one shift width.

**initial.png** — Three lines with no indentation. Cursor at line 1 col 0.

Keys: `>>`

**after_indent_right.png** — Line 1 is indented (shifted right by tab width).
Lines 2 and 3 remain unchanged.
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('>>');
        $ctx->delay(100);
        $ctx->snapshot('after_indent_right');
    },
}
__DATA__
line one
line two
line three
