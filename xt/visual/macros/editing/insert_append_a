{
    desc => q{a appends text after cursor position},
    description => <<'END_DESC',
# insert_append_a, a appends text after cursor position

Macro: `xt/visual/macros/editing/insert_append_a`

## initial.png

- Cursor: 1, 0
- Line 1  : Hello world

## after_append.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->key('a');
        $ctx->type('XY');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_append');
    },
}
__DATA__
Hello world
