{
    desc => 'P pastes yanked text before cursor',
    description => <<'END_DESC',
# paste_before_P, P pastes yanked text before cursor

Macro: `xt/visual/macros/editing/paste_before_P`

## initial.png

- Cursor: 1, 0
- Line 1  : first line
- Line 2  : second line

## on_line2.png

- Keys    : j

## after_P.png

- Keys    : yykP


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('j');
        $ctx->delay(50);
        $ctx->snapshot('on_line2');
        $ctx->delay(100);
        $ctx->keys('yykP');
        $ctx->delay(100);
        $ctx->snapshot('after_P');
    },
}
__DATA__
first line
second line
