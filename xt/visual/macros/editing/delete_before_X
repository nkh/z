{
    desc => q{X deletes character before cursor},
    description => <<'END_DESC',
# delete_before_X, X deletes character before cursor

Macro: `xt/visual/macros/editing/delete_before_X`

## initial.png

- Cursor: 1, 0
- Line 1  : This is a longer line of text for testing deletion.
- Line 2  : The quick brown fox jumps over the lazy dog.
- Line 3  : Another line here to fill the buffer.
- Line 4  : And yet another line for scrolling context.
- Line 5  : Final line of the test buffer content.

## cursor_before_X.png

- Keys    : 5l

## after_X.png

- Keys    : X


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('5l');
        $ctx->delay(100);
        $ctx->snapshot('cursor_before_X');
        $ctx->delay(100);
        $ctx->keys('X');
        $ctx->delay(100);
        $ctx->snapshot('after_X');
    },
}
__DATA__
This is a longer line of text for testing deletion.
The quick brown fox jumps over the lazy dog.
Another line here to fill the buffer.
And yet another line for scrolling context.
Final line of the test buffer content.
