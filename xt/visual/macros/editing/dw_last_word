{
    desc => q{dw on last word of line},
    description => <<'END_DESC',
Action: `dw` on the last word of a line deletes it without trailing whitespace

**initial.png** (before):
- Line 1: `This is a longer line of text for testing.`
- Cursor at line 1, col 0

Keys: `wwdw`

**after_dw_last.png** (after):
- `dw` deletes the word under the cursor
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('wwdw');
        $ctx->delay(100);
        $ctx->snapshot('after_dw_last');
    },
}
__DATA__
This is a longer line of text for testing.
The quick brown fox jumps over the lazy dog.
Another line here to fill the buffer.
And yet another line for scrolling context.
Final line of the test buffer content.
