{
    desc => q{3X deletes 3 characters before cursor},
    description => <<'END_DESC',
Action: `3X` deletes 3 characters before the cursor

**initial.png** (before):
- Line 1: `This is a longer line of text for testing deletion.`
- Cursor at line 1, col 0

Keys: `7l3X`

**after_3X.png** (after):
- 3 characters before cursor position deleted
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('7l3X');
        $ctx->delay(100);
        $ctx->snapshot('after_3X');
    },
}
__DATA__
This is a longer line of text for testing deletion.
The quick brown fox jumps over the lazy dog.
Another line here to fill the buffer.
And yet another line for scrolling context.
Final line of the test buffer content.
