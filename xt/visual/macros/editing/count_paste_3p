{
    desc => '3p pastes 3 copies of yanked text below',
    description => <<'END_DESC',
# count_paste_3p, 3p pastes 3 copies of yanked text below

Macro: `xt/visual/macros/editing/count_paste_3p`

## initial.png

- Cursor: 1, 0
- Line 1  : first line
- Line 2  : second line
- Line 3  : third line

## after_yy.png

- Keys    : yy

## after_3p.png

- Keys    : 3p


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('yy');
        $ctx->delay(100);
        $ctx->snapshot('after_yy');
        $ctx->delay(100);
        $ctx->keys('3p');
        $ctx->delay(100);
        $ctx->snapshot('after_3p');
    },
}
__DATA__
first line
second line
third line
