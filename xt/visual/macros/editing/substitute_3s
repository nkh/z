{
    desc => q{3s substitutes 3 characters},
    description => <<'END_DESC',
# substitute_3s, 3s substitutes 3 characters

Macro: `xt/visual/macros/editing/substitute_3s`

## initial.png

- Cursor: 1, 0
- Line 1  : hello world

## after_3s.png

- Keys    : 3sXX


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('3sXX');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_3s');
    },
}
__DATA__
hello world
