{
    desc => '2>> indents 2 lines right',
    description => <<'END_DESC',
# indent_2lines, 2>> indents 2 lines right

Macro: `xt/visual/macros/editing/indent_2lines`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three

## after_indent_2lines.png

- Keys    : 2>>


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('2>>');
        $ctx->delay(100);
        $ctx->snapshot('after_indent_2lines');
    },
}
__DATA__
line one
line two
line three
