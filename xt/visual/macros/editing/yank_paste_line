{
    desc => 'yy then p yanks a line and pastes it below',
    description => <<'END_DESC',
# yank_paste_line, yy then p yanks a line and pastes it below

Macro: `xt/visual/macros/editing/yank_paste_line`

## initial.png

- Cursor: 1, 0
- Line 1  : first line
- Line 2  : second line

## after_yank_line.png

- Keys    : yy

## after_paste_line.png

- Keys    : p


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('yy');
        $ctx->delay(100);
        $ctx->snapshot('after_yank_line');
        $ctx->delay(100);
        $ctx->keys('p');
        $ctx->delay(100);
        $ctx->snapshot('after_paste_line');
    },
}
__DATA__
first line
second line
