{
    desc => q{J joins current line with the next line},
    description => <<'END_DESC',
# join_lines_J, J joins current line with the next line

Macro: `xt/visual/macros/editing/join_lines_J`

## initial.png

- Cursor: 1, 0
- Line 1  : hello
- Line 2  : world
- Line 3  : foo

## after_join.png

- Keys    : J


END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('J');
        $ctx->delay(100);
        $ctx->snapshot('after_join');
    },
}
__DATA__
hello
world
foo
