{
    desc => q{C changes to end of line},
    description => <<'END_DESC',
Action: `C` deletes from cursor to end of line and enters insert mode

Macro: `xt/visual/macros/change_eol_C`

**initial.png** (before):
- Line 1: `Hello World Test`
- Cursor at line 1, col 0

Keys: `6lC`, type `DONE`, `Escape`

**after_C.png** (after):
- Line 1: `Hello DONE` (`World Test` replaced with `DONE`)
- Cursor at line 1, col 9 (after `DONE`)
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('6lC');
        $ctx->delay(50);
        $ctx->type('DONE');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_C');
    },
}
__DATA__
This is a first line of text in the buffer.
Here is a second line with more content.
Third line with some additional words here.
Fourth line to provide scrolling context.
Fifth line at the bottom of the buffer.
