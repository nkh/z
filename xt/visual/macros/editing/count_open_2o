{
    desc => '2o opens 2 new lines below',
    description => <<'END_DESC',
# count_open_2o, 2o opens 2 new lines below

Macro: `xt/visual/macros/editing/count_open_2o`

## initial.png

- Cursor: 1, 0
- Line 1  : line one
- Line 2  : line two
- Line 3  : line three

## inserting.png

- Keys    : 2o

## after_escape.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('2o');
        $ctx->delay(50);
        $ctx->type('new');
        $ctx->delay(100);
        $ctx->snapshot('inserting');
        $ctx->delay(50);
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_escape');
    },
}
__DATA__
line one
line two
line three
