{
    desc => q{x deletes character under cursor},
    description => <<'END_DESC',
Action: `x` deletes the character under the cursor

**initial.png** (before):
- Line 1: `This is a longer line of text for testing.`
- Cursor at line 1, col 0 (on `T`)

Keys: `x`

**after_delete.png** (after):
- Line 1: `his is a longer line of text for testing.` (`T` deleted)
- Cursor at line 1, col 0 (on `h`)
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('x');
        $ctx->delay(100);
        $ctx->snapshot('after_delete');
    },
}
__DATA__
This is a longer line of text for testing.
The quick brown fox jumps over the lazy dog.
Another line here to fill the buffer.
And yet another line for scrolling context.
Final line of the test buffer content.
