{
    desc => q{i enters insert mode and typing inserts text before cursor},
    description => <<'END_DESC',
# insert_typing, i enters insert mode and typing inserts text before cursor

Macro: `xt/visual/macros/editing/insert_typing`

## initial.png

- Cursor: 1, 0
- Line 1  : world

## after_typing.png



END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->key('i');
        $ctx->type('Hello ');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_typing');
    },
}
__DATA__
world
