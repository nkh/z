{
    desc => q{cw changes word},
    description => <<'END_DESC',
Action: `cw` deletes the word under the cursor and enters insert mode

**initial.png** (before):
- Line 1: `This is a longer line of text for testing changes.`
- Cursor at line 1, col 0 (start of `This`)

Keys: `cw`, type `REPLACED`, `Escape`

**after_cw.png** (after):
- Line 1: `REPLACED is a longer line of text for testing changes.` (`This` replaced with `REPLACED`)
END_DESC
    run => sub {
        my ($ctx) = @_;
        $ctx->buffer->set_text($CODE);
        $ctx->delay(100);
        $ctx->snapshot('initial');
        $ctx->delay(100);
        $ctx->keys('cw');
        $ctx->delay(50);
        $ctx->type('REPLACED');
        $ctx->key('Escape');
        $ctx->delay(100);
        $ctx->snapshot('after_cw');
    },
}
__DATA__
This is a longer line of text for testing changes.
The quick brown fox jumps over the lazy dog.
Another line here to fill the buffer.
And yet another line for scrolling context.
Final line of the test buffer content.
